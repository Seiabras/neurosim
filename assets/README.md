# assets/ — imagens

`char-*.png`, `maria.png`, `par-*.png`, `sec-*.png`: retratos dos pacientes, acompanhantes e visitantes (usados na consulta e no mapa).
`psi-*.png`, `psicologa*.png`: partes da personagem. `icon-*`: ícones dos botões. `tex-*.js`: texturas embutidas do 3D. `3d/`: fontes das texturas.
O PDF do manual DSM-5-TR e a imagem `WhatsApp*` **não vão para o GitHub** (estão no `.gitignore`).
